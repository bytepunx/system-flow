#!/usr/bin/env sh
# Integration tests: one service against real adapters. Run after behavior tests pass.
# Replace the body with this project's command; keep the exit code honest.
set -eu
cd "$(dirname "$0")/.."
echo "integration.sh: no tests configured yet; see design/conventions/code-quality.md"
