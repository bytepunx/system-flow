default: true
MD013: false          # line length
MD024:
  siblings_only: true # duplicate headings allowed in different sections (log entries)
MD033: false          # inline html, used for the baseline marker and mermaid
MD041: false          # first line heading, front matter comes first
