---
stream: S-024
title: Prime agent sessions from CLAUDE.md
updated: 2026-09-16T22:46:09Z
agent: claude-fable-5-1
session: 5da6af50
---

# S-024 Prime agent sessions from CLAUDE.md

## Context
CLAUDE.md is the map, design/conventions are the norms. Today the baseline CLAUDE.md.tmpl still carries narrative obligations, a definition of done, and a start-of-session procedure inline, duplicating session-start.md and work-management.md. This story makes the template CLAUDE.md open with a priming section that sends the agent to the conventions first and shrinks the rest to the layout table and pointers. This repository's CLAUDE.md is re-rendered from it with the About this repository section preserved below the marker.

## Current state
All three tasks done, story in review. template/root/CLAUDE.md.tmpl is 50 lines: title, one-line map, Prime your session (read order, precedence, conflict handling), layout table including conventions, issues, and scripts, work-items pointer with the CLI commands, marker. Root CLAUDE.md re-rendered from it with the About this repository section preserved and its flai line updated to scripts/flai.sh. Verified: template-test, prototype render test, flai check --strict (84 items), markdownlint on a fresh render. Committed at review and pushed.

## Next steps
1. Operator accepts S-024; acceptance triggers archive, commit, and a template patch bump note (no sub-project code touched, so no flai tag).
2. Next pull: S-025 (flai prime, check rules for conventions), then S-027 (flai issue).

## Decisions
- CLAUDE.md is a map: everything that is a rule now lives in a convention file and CLAUDE.md links to it. The only procedural text kept is the priming order, because it must be read before the conventions are.
- Precedence and conflict handling are stated in CLAUDE.md as well as conventions/README.md, deliberately: an agent that reads only CLAUDE.md still knows the operator wins and that deviations are logged, never silent.
- Description lines in CLAUDE.md, README, and overview templates are conditional so an empty description does not leave double blank lines that fail markdownlint in a new project.

## Open questions

## Log

### 2026-09-16T22:44:07Z
Stream opened.

### 2026-09-16T22:46:09Z
Template CLAUDE.md rewritten: priming section first (conventions in order, streams index, board), precedence and conflict rules, layout table, work-items pointer; narrative obligations and definition of done removed in favour of the convention files. Root CLAUDE.md re-rendered with its project section preserved. Fresh render is markdownlint-clean and passes flai check; description lines made conditional in three templates.
