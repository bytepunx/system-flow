---
stream: S-023
title: Write the baseline convention set
updated: 2026-09-15T18:43:49Z
agent: claude-fable-5-1
session: 5da6af50
---

# S-023 Write the baseline convention set

## Context
Writes the nine baseline convention files and their README index in template/root/design/conventions/, following the format in design/system/conventions.md and ADR-0013. Scope is the template only: the operator will review and edit the files there, then ask for S-026 to copy them into this repository and apply them. The files are copied verbatim by flai new (no .tmpl), so they must not contain template syntax and must use the default folder names.

## Current state
All four tasks done, story in review. template/root/design/conventions/ holds README.md plus nine topic files (session-start 10, communication 20, work-management 30, decisions 40, documentation 50, code-quality 60, git 70, safety 80, tooling 90), each with front matter, a Rules section, a When in doubt section, the baseline marker, and an empty Project additions section. Verified: flai new renders them, flai check --strict passes on the result, no template syntax, prototype tests pass. This repository does not yet have the folder; that is S-026, pulled when the operator asks after editing the template.

## Next steps
1. Operator reviews and edits the files in template/root/design/conventions/ directly.
2. Operator accepts S-023 and, when ready, asks for S-026 (copy into this repo, add project additions, align practice).
3. If S-026 is not requested, the next pull is S-024 (CLAUDE.md priming section).

## Decisions
- Files are verbatim copies (no .tmpl) using default folder names, so the same text reads identically in the template repo and in projects; a project that renames layout folders edits below the marker.
- Each file is Rules plus When in doubt; rationale is a link, never a paragraph. Longest file is under 60 lines against the 120 limit.
- The safety file forbids fabricating timestamps and results outright, and the tooling file forbids hand-editing front matter a command owns; both codify lessons from this project's own history.
- README.md carries the precedence rule and the marker explanation so an agent that reads only the index still knows how the files work.

## Open questions

## Log

### 2026-09-15T18:40:51Z
Stream opened.

### 2026-09-15T18:43:49Z
Ten files written in template/root/design/conventions (341 lines total, longest under 60). flai new renders them into a project that passes flai check --strict; prototype tests pass. Moved to review for the operator's edits.
