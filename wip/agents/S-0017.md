---
stream: S-0017
title: flai reference
updated: 2026-09-24T08:19:16Z
agent: system-flow
session: 20260924T081135
---

# S-0017 flai reference

## Context
S-0017 (E-0004): `docs/users/flai.md` documents every command, flag, and the config file; the criterion is a reference generated from the cobra help and reviewed. Tasks: T-0400 generator, T-0401 review the generated page and fix the help text, T-0402 document the config file and link the reference.

## Current state
T-0400 and T-0401 done and committed on `story/S-0017`. T-0402 edits are in the worktree, uncommitted: the Configuration section of `docs/users/flai.md` documents every key (plain keys, the keys `flai serve` manages, environment variables), the retired push key paragraph is corrected, `index.md` and `flai.md` link `docs/users/flai-reference.md`, and `design/system/flai-cli.md` describes `flai reference`. `make lint-md` passes in the worktree. Running `make flai-test` next.

## Next steps
1. `make flai-test` in the worktree; fix anything it finds.
2. Commit T-0402, move it to done, `flai stream sync`.
3. `make lint-md` in the main checkout, `flai check --strict`, tick the criterion, close the narrative, `inbox`, move S-0017 to review.

## Decisions
- The reference is its own page, `docs/users/flai-reference.md`, linked from `flai.md`: about 2,000 generated lines would bury the guide, and a separate file lets the generator own the whole file. `flai.md` stays the hand-written guide and documents the config file.
- A hidden `flai reference` command renders the page with the standard library, not `cobra/doc`: no new dependency (code-quality.md), and the output is shaped for this repo's markdown lint and anchors.
- `--write` rewrites the file only when the body changed, so `updated` moves only then and the staleness test compares bodies, not dates.
- `docs/users/flai.md` goes from `status: draft` to `active`: it was a draft for want of the generated reference, which this story adds.
- One commit per task, as the operator's start instruction for this session says, where `git.md` says commit when the story lands; the operator's instruction takes precedence.

## Open questions

## Log

### 2026-09-24T08:12:05Z
Stream opened.

### 2026-09-24T08:15:56Z
Session started by flai serve for S-0017. Pushed the pending S-0016 acceptance (flai push --pending). Moved S-0017 to in-progress, wrote T-0400..T-0402, tagged the story cli. T-0400 done: flai reference generator, script, make target, tests.

### 2026-09-24T08:19:16Z
T-0401 done: reviewed the generated reference end to end and fixed the help it exposed (items, move, push, release, config). T-0402 in progress: config file documented, reference linked; running make flai-test.
