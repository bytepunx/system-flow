---
stream: S-0118
title: flai serve project add, remove, and list manage the projects the host serves and say why one is not showing
updated: 2026-09-26T05:39:19Z
agent: agent-S-0118
session: 20260926T052422
---

# S-0118 flai serve project add, remove, and list manage the projects the host serves and say why one is not showing

## Context

Started by flai serve on the host (`FLAI_STARTED_BY=flai-serve`). The registry is `serve/projects.json` beside flai's config (`internal/serve/serve.go`); only `flai dashboard` (`cmd/dashboard_agent.go`) and the board's import (`internal/serve/candidates.go`) write it. S-0117 runs at the same time and also touches `flai/internal/serve` and `flai/cmd/import.go`; this story keeps its new code in new files (`internal/serve/projects.go`, `cmd/serve_project.go`) so the two merge cleanly.

The dashboard keeps a hub for every project that ever connected and lists it as not connected once flai goes (`flaiover/src/lib/server/agent.ts` `list()`), so unregistering alone does not drop a project from the switcher. That is what T-0434 is for.

## Current state

All four tasks are done and committed on `story/S-0118` (T-0433 to T-0436 as five commits, then a formatting fix and a wording fix). Every acceptance criterion is met and verified: `scripts/flai-test.sh` (lint, all Go tiers) and `scripts/flaiover-test.sh` (473 tests) pass. `flai check --strict` has two warnings and no errors, both `wip.overlap` with S-0117 on `flai/internal/serve`, which is real. A manual run with a scratch `FLAI_CONFIG` exercised add, the key refusal, a moved folder shown as unavailable in `list` and `serve status`, and remove.

`git merge-tree` of `story/S-0117` against `story/S-0118` conflicts in `design/system/flai-cli.md`, `docs/users/flai.md`, `flai/cmd/dashboard_agent.go`, `flai/cmd/serve.go`, and `flai/internal/serve/serve.go`. S-0117 also added `Status.ImportProjects` and `Status.Unserved []serve.Found` and `cmd`'s `belowImportRoots`, which serve the projects below the import folders that `flai serve project list` here lists as unserved from its own `workitem.FindProjects` scan. Once S-0117 is on main, list should take them from there. The question of order is open on a thread.

## Next steps

1. Wait for the answer on the thread about S-0117 (`wait_for_events`).
2. If S-0117 is accepted first: `flai stream sync S-0118`, resolve the five conflicts (keep S-0117's `serveEntry`/`import_serve.go` alongside `registerProject`, both `Status` fields, and both reconcile changes), make `listServedProjects` count `Status.ImportProjects` as served and take the unserved from S-0117's `Found`, rerun both test scripts, then `flai move S-0118 review`.
3. Otherwise move S-0118 to review as it is, and say in the report that whichever is accepted second must be rebased.

## Decisions

- `flai serve project add` requires `system-flow.yaml` in the folder itself, the current one by default, rather than walking up to the project above: the criterion asks it to refuse a folder with no manifest. `remove` does walk up, so `.` works anywhere in the project.
- An unreadable agent credential counts as a reason a project cannot be served, logged once, as a missing folder is: it was the other message `flai serve` repeated every second.
- A key another root already has is refused in `serve.Dir.Register` itself, so `flai dashboard` and the board's import refuse it too, not only `flai serve project add`. Two roots with one key would take turns replacing each other's connection in the dashboard.
- flai serve tells the dashboard a project was removed with a notification, `removed`, sent before the connection is dropped, rather than the dashboard forgetting every project that disconnects: a project whose flai is only restarting must stay in the switcher.
- `flai serve project remove` does not stop the dashboard container, unlike `flai dashboard stop`: it manages what is served, not the container.

## Open questions

## Log

### 2026-09-26T05:24:40Z
Stream opened.

### 2026-09-26T05:26:31Z
Session started by flai serve for S-0118. Found main with S-0117 in progress (another agent) and S-0118 ready; pulled it, wrote T-0433 to T-0436, started T-0433.

### 2026-09-26T05:39:19Z
T-0433 to T-0436 done; all criteria verified. Found that story/S-0117 and story/S-0118 conflict in five files and that S-0117's Status.ImportProjects and Unserved overlap list's unserved section; asking the designer about order before review.
