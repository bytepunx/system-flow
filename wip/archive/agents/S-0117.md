---
stream: S-0117
title: Every project's cache on the dashboard forgets what flai says changed
updated: 2026-09-26T03:33:34Z
agent: agent-S-0117
session: 3db20146
---

# S-0117 Every project's cache on the dashboard forgets what flai says changed

## Context

flaiover keeps one Repo (cache of flai's answers) per project key. Only the default project's Repo was watched at startup, so `/api/board?project=sf` kept stories that had been archived until the container restarted. Fix: `repo()` watches every Repo it makes.

## Current state

Both tasks are done and committed on `story/S-0117`: the fix in `flaiover/src/lib/server/repo.ts` with tests in `project-events.test.ts` and a note in `design/system/flaiover-dashboard.md` (da61e6f), I-0046 recorded (3509789), and I-0045 bumped (403c788). Every criterion is ticked. The flaiover suite, `flai check --strict`, the markdown lint, and the template smoke pass. `make test` and `make smoke`'s install-test fail only the I-0045 macOS path checks, in flai code this story does not touch. The story is moving to review.

## Next steps

1. Operator: accept S-0117 (`flai accept S-0117 --by alex`); flaiover takes a patch at the next publish.

## Decisions

- Commit per task rather than once at review: the operator's instruction for this session says so, which takes precedence over `git.md`.
- Left `hooks.server.ts`'s startup `watch()` on the default Repo in place: now redundant but harmless, and removing it is not in the story's scope.

## Open questions

None.

## Log

### 2026-09-26T03:29:23Z
Stream opened.

### 2026-09-26T03:33:34Z
Session started fresh (story was ready, no branch). Wrote T-0435 and T-0436, fixed repo() to watch every Repo with a test that failed first, recorded I-0046, bumped I-0045 for the macOS failures in make test and make smoke. All criteria verified; moving to review.
