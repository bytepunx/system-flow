---
stream: S-0069
title: Research tunnel options for reaching flairport from outside the network, ending in a story for flai CLI integration if one suits
updated: 2026-09-20T06:15:05Z
agent: system-flow
session: faa60a41
---

# S-0069 Research tunnel options for reaching flairport from outside the network, ending in a story for flai CLI integration if one suits

## Context
Research for E-0007's second outcome: put flairport on the internet through a tunnel whose provider ends HTTPS and checks identity at its edge. The output is a finding under `design/system`, the operator's decision, and a flai CLI integration story if one or more options suit. Nothing is built here. The operator named this story and had placed it first in the ready order. Model for the shape: S-0052 (archived narrative).

Constraints that bind the trials: scratch services and scratch containers only; never the operator's dashboards, accounts, keys, domains, or this repository's remote; nothing under `~/.flai`.

## Current state
Cancelled by the operator on 2026-09-20T06:11:55Z: "I am thinking of taking a very different route". Stopped there. T-0245 and T-0246 were done; T-0247 and T-0248 were cancelled with the story (by hand: the gap S-0070 closes). The finding as far as it got is one unmerged commit on `story/S-0069` (`design/system/tunnel-options.md`: needs, candidates, trials, security, a recommendation, decision pending) and was sent to the operator as a file. Nothing of it is on main. The branch and worktree are left alone for the operator to keep or remove. The scratch lab is removed.

## Next steps
1. None. If the different route still wants a tunnel, the finding on `story/S-0069` is the place to start.

## Decisions
- 2026-09-20: the operator cancelled the story while the decision questions were open; the questions below were never answered and are moot.

## Open questions
- For the operator (asked 2026-09-20, not yet answered): which way should the integration story go: Cloudflare Tunnel + Access (recommended), Cloudflare and Tailscale Serve, Tailscale Serve only, or self-hosted Pangolin?
- For the operator: is there a domain with its DNS on Cloudflare to use for flairport, a domain elsewhere, or none yet?
- For the operator: the sign-in at the edge: GitHub sign-in (passkey there), email code plus Access's WebAuthn second factor, email code only, or leave it to the story?

## Log

### 2026-09-20T04:06:37Z
Stream opened.

### 2026-09-20T04:18:50Z
Operator's answers (2026-09-20, in session). Today: no tunnel is set up for the dashboard yet; 'I either misspoke or you misinterpreted' the S-0052 answer. They have used cloudflared before for other things, but want to see what is available before taking that path, prioritising low or no cost. Cost: an overview of trade-offs for our use; a few dollars a month is the ceiling if possible, but if the cheap options have too many drawbacks or security concerns, look at something else. Who: only them. Remote MCP: no, agents run on the host, so the edge can block /mcp outright.

### 2026-09-20T04:26:49Z
T-0245 done: four documentation readers (Cloudflare, Tailscale, ngrok, self-hosted and others) plus the needs from flaiover's code (SSE with 25 s ping, NDJSON acceptance, Origin-against-Host check, bearer-only /mcp). T-0246 done: scratch service in its own docker network; Cloudflare quick tunnel buffers NDJSON and delivers no SSE (matches its docs; QUIC and HTTP/2 alike), bearer and Host pass; Caddy+forward_auth stub -> frps 0.71.0 -> frpc streams line by line, idle SSE held 200 s, gate holds, /mcp bypass works. Tailscale, ngrok, dev tunnels, Pangolin, and a named Cloudflare tunnel need accounts or a domain: not tried. Lab removed. Finding drafted at design/system/tunnel-options.md and committed on the story branch. Found on the way: Microsoft dev tunnels (free, private by default, no domain) and that authstar's portcullis puts its internal token in Authorization.

### 2026-09-20T06:15:05Z
Story cancelled by the operator (06:11:55Z, 'I am thinking of taking a very different route'). Open tasks T-0247 and T-0248 cancelled by hand with that cause. The unmerged finding stays on story/S-0069; nothing reached main. Stream closed.
