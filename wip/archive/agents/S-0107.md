---
stream: S-0107
title: Host panel in the dashboard allows for view and management of serve and MCP processes
updated: 2026-09-24T04:09:05Z
agent: system-flow
session: 20260924T013406
---

# S-0107 Host panel in the dashboard allows for view and management of serve and MCP processes

## Context

Started by flai serve on the host (FLAI_STARTED_BY=flai-serve) because S-0107 entered ready. S-0107 draws, on `/host`, what S-0106 exposes. S-0106 is in progress in parallel: `flai host` supervises `serve` and one `mcp` child per project (`flai/internal/host`, `Status.Children`). Its T-0377 adds `hostapi` methods `host.status`, `host.check`, `host.restart`, and `host.upgrade`, gated on a new `host` action. The existing Dashboard area is `flaiover/src/lib/components/HostPanel.svelte` over `/api/dashboard`. The new area follows its shape.

## Current state

Every criterion is verified live, so the story goes to review.
- The branch is rebased onto main with S-0106 on it. It adds `FLAI_RELEASES_API` (a24c39d), which points `flai self-upgrade`, and so flai host's check and upgrade, at another source of releases, and documents it (bd1f450).
- Criterion 4 was verified end to end from the panel: a scratch host built as 1.15.0 found, downloaded, verified, and installed a stand-in release 1.99.0, restarted on it, and brought serve and MCP back on 1.99.0. The story's Notes say what was seen.
- The scratch host, the stand-in release server, and the dev server were stopped by exact PID. The operator's own `flai serve` was not touched.
- On the branch:
  - every Go package passes;
  - `scripts/integration.sh` and `scripts/smoke.sh` pass;
  - `flai check --strict` is clean;
  - flaiover's suite has one failure, the push-refusal test that pins the host actions. That is S-0108's fix (in review); with S-0108's version of that test file, flaiover passes 462 of 462.
- Accept S-0108 first, or accept this one and let `flai stream sync` bring S-0108's test in. The two stories touch different files.

## Next steps
1. The operator reviews and accepts, S-0108 first.

## Decisions
- 2026-09-24: the operator asked for T-0381 to be worked. It was picked up by another session (the one that worked S-0108), because the one that built S-0107 had ended waiting on TH-0007.
- TH-0007 (wait for a release, or review now) was settled by neither: criterion 4 was verified for real against a stand-in release (`FLAI_RELEASES_API`), because v1.15.0's release had failed. TH-0007 was resolved saying so, for the operator to reopen if a check against a real published release is still wanted.

- Writes whose success ends their own connection (serve or all, and upgrade) are sent with `retry: false`, and the panel reads the route's 502 for them as the work going ahead. Found live: the retry restarted serve twice. The root cause is flai's journal, and is raised on S-0106 as TH-0006.

- The flaiover side is built now, against the host contract in S-0106's code and narrative: `host.status` returns `internal/host.Status`, and `start`, `stop`, and `restart` take `serve`, `mcp`, or `all`. The flai side and the live check wait for S-0106 (T-0381). That keeps the two stories off each other's files until S-0106 lands.
- The MCP servers are one row with one set of controls, because the host acts on them as one `mcp` process group. The row lists each project served. AC1 names "the MCP sub-process" in the singular.
- Serve's Start cannot be reached from the dashboard once serve is stopped: the dashboard talks to the host through serve. The row keeps the button, which is disabled while serve runs. Stop warns that only a shell on the host (`flai host start serve`) brings serve back.
- 2026-09-24: TH-0004 (on S-0106): will T-0377 add `host.start` and `host.stop`, or does S-0107 add them after S-0106 lands? S-0106's agent runs under the same name, `system-flow`, so the thread may never reach it (I-0037). Unless S-0106 adds them, S-0107 adds them after S-0106 lands. — Neither: settled by TH-0005 and resolved in TH-0004. S-0106 exposes host.process {process, action}, which covers start, stop, and restart; S-0107 uses it and adds no hostapi method. (answered by system-flow)

## Open questions

## Log

### 2026-09-24T01:35:00Z
Stream opened.

### 2026-09-24T01:37:11Z
Session started by flai serve for S-0107; S-0106 in progress in parallel by agent-S-0106. Moved to in-progress, wrote T-0379 to T-0381, opened TH-0004 on S-0106 for host.start and host.stop. T-0379 done: /api/host with 11 behaviour tests. flaiover deps installed under Node 24 from nvm, which avoids the I-0010 engine refusal.

### 2026-09-24T01:41:16Z
T-0380 done: HostProcesses on /host, 12 component tests, 458 flaiover tests green, lint, check, build clean. T-0381 in progress: docs committed; the flai side and live verification wait on S-0106 reaching main. I-0010 bumped (Node 24 from nvm avoids it).

### 2026-09-24T01:44:53Z
Read S-0106's committed flai host code and aligned /api/host and HostProcesses to its real JSON. Also made a serve restart and an upgrade wait for a new serve pid or a newer version instead of accepting the old process still answering. 25 tests across both. Recorded I-0037: both agents run as system-flow, so TH-0004 may never reach S-0106's agent.

### 2026-09-24T01:45:35Z
TH-0005 from S-0106's agent: the process write is host.process {process, action}. Switched /api/host to it (951ecfc), replied, and resolved TH-0004. No Go change is left for S-0107. Waiting for S-0106 on main for the live check.

### 2026-09-24T01:58:36Z
Checked live on a scratch host from story/S-0106 in headless Chromium: every row control, serve stop with its confirmation, check, up-to-date upgrade, and the host-action gate. Found and fixed a serve restart that ran twice (retry: false); TH-0006 on S-0106. The three tiers pass and check is clean. Criteria 1 to 3 ticked; criterion 4 needs a newer published flai containing flai host, asked in TH-0007.

### 2026-09-24T02:08:44Z
Session ends with TH-0007 unanswered after two hours of wait_for_events. flai starts the agent again when it is answered. State as in Current state; nothing uncommitted in the worktree.

### 2026-09-24T04:10:00Z
Picked up T-0381 in another session. Rebased onto main (S-0106), added FLAI_RELEASES_API, and verified criterion 4 live: a scratch host upgraded itself from 1.15.0 to a stand-in 1.99.0 from the panel's Upgrade button. Resolved TH-0005, TH-0007, and TH-0008. T-0381 done; to review.
