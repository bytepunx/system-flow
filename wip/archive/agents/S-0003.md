---
stream: S-0003
title: Publish the template as its own repository
updated: 2026-09-17T00:11:38Z
agent: claude-fable-5-1
session: 5da6af50
---

# S-0003 Publish the template as its own repository

## Context
The template has lived in ./template since the first session and is now at 1.0.0 with E-0005 closed. This story publishes it as github.com/bytepunx/system-flow-template, the default source in flai's config, so projects outside this monorepo can `flai new`. S-0021 (flai template push) will automate the sync later; this story does the first publish by hand with git subtree so the template's history is preserved without rewriting this repository. The git convention requires asking the operator for visibility before creating a remote.

## Current state
All three tasks done, story in review. github.com/bytepunx/system-flow-template exists (private), main carries the template's 17-commit history via git subtree split, tag v1.0.0 matches template.yaml. flai new from the published repo at v1.0.0 renders the same 48 files as ./template and passes check. docs/contributors documents the hand sync until S-0021; template.md records the publish; S-0021's note updated. Side finding: flai's default cache_dir put a clone under ~/.flai; the repo config now sets cache_dir inside .flai-cache, scripts/env.sh sets it on first run, the stray clone was removed, I-009 recorded. Committed at review and pushed.

## Next steps
1. Operator accepts S-0003; nothing to tag (delivered to the template, already at 1.0.0 and published).
2. Next per board order: the remainder of E-0002 (S-0006 import, S-0009 dashboard, S-0020 upgrade, S-0021 push, S-0029 release) and E-0003; operator to set the order.

## Decisions
- Published by subtree split rather than a fresh commit so the template repo has real history; tags there are plain vX.Y.Z, tags here stay prefixed.
- This repository keeps template.repo as the local path; other projects use the published repo and a tag.
- cache_dir for this repo lives under .flai-cache so nothing is written to the operator's home.

## Open questions

## Log

### 2026-09-17T00:07:13Z
Stream opened.

### 2026-09-17T00:11:38Z
Created bytepunx/system-flow-template (private, operator's answer) with gh; pushed the subtree history of ./template (17 commits) as main and tagged v1.0.0. Rendered a project from the published repo at v1.0.0 and from ./template: identical apart from name, ref, and timestamps; both pass check. Sync procedure documented in docs/contributors. Found and fixed flai caching a clone under ~/.flai (I-009).
