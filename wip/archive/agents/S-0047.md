---
stream: S-0047
title: "Issue instances written in the same second share a heading; release delivers to a component the story touched"
updated: 2026-09-19T10:35:14Z
agent: system-flow
session: faa60a41
---

# S-0047 Issue instances written in the same second share a heading; release delivers to a component the story touched

## Context
Two small flai defects that each broke something on main. `flai issue bump` and `flai issue new` write a `### <timestamp>` heading per instance, and two in the same second gave duplicate headings that failed the markdown lint on main (I-0011, second occurrence). And the release awards the delivery bump to the first component a story's tags name, touched or not: S-0039, pure flai work tagged [dashboard, cli], gave flaiover a minor with zero files and flai a patch (I-0016); S-0035 and S-0037 had the same shape. Versions stand as cut. Work is in `.flai-cache/worktrees/S-0047` on `story/S-0047`.

## Current state
Done on `story/S-0047` in three commits (b18dcc6, 8ca41f9, 00a02c9), ready for review.

Issues: `insertInstance` in `flai/internal/issues` writes a new `### <timestamp>` heading only when the last instance does not already carry that timestamp; otherwise the note goes under it as a further paragraph. `flai issue new` followed by a bump in the same second is the same path. `TestInstancesInTheSameSecondShareOneHeading` bumps twice at one instant, twice over, checks that no heading repeats, every note is kept in order, and the count is five; against the old code it fails with both headings doubled.

Release: `deliverTarget` takes `--deliver` first, then the story's tags and then its epic's, and only among the components the commits touched; the most touched files win, the earlier tag breaks a tie; a tag naming an untouched component never delivers, so that component gets no release at all, and a tagged story that touched no component releases nothing, where it used to get a release with no files. With no tag deciding, the only touched component delivers and several still ask for a tag or `--deliver`. `TestDeliveryGoesToATouchedComponent` covers each case; against the old code it reproduces I-0016 exactly (`web minor delivered, cli patch incidental` for CLI-only work tagged [web, command]). `make flai-test` passes, 0 lint issues.

Tags: no E-0006 story is open any more (S-0059, S-0060, S-0062, S-0063, and S-0064 were tagged and accepted, S-0056 cancelled). The open stories are the E-0004 documentation stories (S-0016 to S-0019, S-0028), which write under `docs/` and touch no component, so they release nothing whatever their tags; none were changed. Documents: the release paragraph in `docs/users/flai.md`, the `flai release` row in `design/system/flai-cli.md`, and the project's release note in `design/conventions/git.md` (below the marker; the baseline does not describe how the component is chosen). I-0011 and I-0016 closed on the branch.

## Next steps
1. The operator reviews and accepts. Then `git fetch`, `scripts/flai.sh push --pending`, wait for the release workflow; no dashboard change, but the image bundles flai, so pull and restart when a flaiover release follows.
2. The ready column is empty after this. Backlog: the E-0004 documentation stories.

## Decisions
- 2026-09-19, agent: pulled without being named because it was the only ready story, nothing was in progress, and the operator's standing word today is to work through the ready stories.

## Open questions
- I-0024 (a story with no component tag that touches two components cannot be accepted, and the board cannot pass `--deliver`) is not closed by this: with no tag deciding and several components touched, the release still asks. Letting the most touched component deliver when no tag decides would close it from the board, at the price of a minor release landing on a component nobody named; the operator choosing the delivered component in the acceptance confirmation would close it without guessing. Worth a small story under E-0006.
- The "remaining E-0006 stories" criterion had nothing left to tag by the time this story was pulled.

## Log

### 2026-09-19T10:29:15Z
Stream opened.

### 2026-09-19T10:35:14Z
T-0242 to T-0244 done: same-second issue instances share a heading; the release delivers only to a touched component, the most touched among tagged ones; both tests fail against the old code, the second reproducing I-0016 exactly; documents; I-0011 and I-0016 closed on the branch. flai-test passes. Also this turn: S-0064's acceptance pushed with flai push --pending (flai 1.4.2) and the operator's dashboard restarted with the tree's flai, so its git hooks, config and info are now read-only in the container, checked in the real container. Moving to review.
