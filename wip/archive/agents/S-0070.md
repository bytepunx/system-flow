---
stream: S-0070
title: Cancelling an epic cancels its open stories and their tasks, and cancelling a story cancels its open tasks
updated: 2026-09-20T06:33:48Z
agent: system-flow
session: faa60a41
---

# S-0070 Cancelling an epic cancels its open stories and their tasks, and cancelling a story cancels its open tasks

## Context
Raised by the operator on 2026-09-20 to supersede S-0069, which they cancelled at 06:11:55Z ("I am thinking of taking a very different route"); its open tasks stayed open, which is the gap. Cancelling an epic or a story must close everything open under it. `Repo.Transition` (`flai/internal/workitem/transition.go`) is the one door: `flai move`, MCP `item_move`, and the dashboard (through `flai move`) all use it. Moves are not committed by anything; they ride with the next commit.

## Current state
All five tasks done; five commits on `story/S-0070` (fcc4a83 core and ADR-0028, bcb8774 CLI and MCP, 1cf32df check rule, 894f8ec dashboard, e96e33a docs, conventions, stats test, I-0025). `make flai-test`, `make flaiover-test`, and `make flaiover-build` pass; `flai check --strict` is clean. Checked in a browser against a scratch project from the item page and from the board. In review, waiting for the operator.

What the operator should know when reviewing: a story in review is cancelled with its epic and called out in the confirmation, and cannot be cancelled directly (ADR-0028); E-0007 still has S-0065 to S-0068 in ready, which is what this story makes one cancellation of, if that is the different route; the MCP server runs the installed flai, so `item_move` and the cause in `inbox` arrive with the next install, while `flai move` from the tree and the dashboard image carry it from the release.

## Next steps
1. After acceptance: push what the board could not, publish the template (its conventions changed), wait for the release workflows, pull the image, restart the dashboard with the tree's flai.

## Decisions
- 2026-09-20: the documentation criterion named the operators' documentation; cancellation is what users of flai and of the board do, and `docs/operators` is about running the dashboard. Documented in `docs/users` and the criterion reworded.
- 2026-09-20: the dashboard's preview runs `flai move --dry-run` with the placeholder reason `preview`, because flai validates the move (reason included) before it lists anything, and the designer types the reason in the same dialog.
- 2026-09-20: `Change.Cause` is derived (an ancestor cancelled at the same timestamp), not stored: no change to the item schema.
- 2026-09-20: pulled on the operator's word ahead of the ready order, and moved to ready and placed first by me for that reason ("a new story that supersedes the current WIP").
- 2026-09-20: a story in review is cancelled by a parent's cascade and called out in the preview; cancelling it directly stays refused (ADR-0004 unchanged for direct moves). The operator asked for all stories to go, refusing would make them resolve each review first, and nothing is lost: the branch stays. To be recorded as an ADR.
- 2026-09-20: a cascaded child's note names the item whose cancellation caused it (the epic, for a task two levels down), not its direct parent. Story criterion reworded to match.
- 2026-09-20: the criterion about one autocommit was wrong about the present: no move is committed by flai or the dashboard. Reworded before starting.

## Open questions

## Log

### 2026-09-20T06:14:35Z
Stream opened.

### 2026-09-20T06:23:30Z
T-0249 done: CancelPlan and TransitionAll in workitem, validate-then-write, review cancelled only through a parent; ADR-0028 recorded. T-0250 done: flai move cancelled previews, asks on a terminal (injectable confirm for tests), --dry-run, --json with cancelled and left_behind; item_move returns cancelled; Change.Cause derived from equal timestamps up the parent chain, summaries say 'was cancelled with E-nnnn'; MCP instructions tell an agent to stop. Commits fcc4a83, bcb8774.

### 2026-09-20T06:30:43Z
T-0251 done (1cf32df): item.parent-cancelled, and item.sequence accepts review to cancelled only with a parent cancelled at the same time. T-0252 done (894f8ec): dry_run on the move endpoint, CancelConfirm on the board and item page; checked in a browser against a scratch project: epic with six open items (one in review, one with a branch and worktree) from the item page, Keep it changes nothing, confirm cancels all and leaves the done task; a story dropped on cancelled from the board takes its task. Mistake: pkill -f 'node build' for the scratch server also stopped the operator's dashboard from listening; restarted it, recorded I-0025 and a memory.

### 2026-09-20T06:33:48Z
T-0253 done (e96e33a). Full suites pass. Moving to review.
