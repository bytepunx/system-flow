---
stream: S-0053
title: "Research and experiment stories can be accepted: findings land on main and are pushed, with no release"
updated: 2026-09-19T07:24:00Z
agent: system-flow
session: faa60a41
---

# S-0053 Research and experiment stories can be accepted: findings land on main and are pushed, with no release

## Context
Acceptance refuses research and experiment stories outright: `release.LevelFor` returns an error for both natures, `release.Compute` calls it first, so the dry run and the real acceptance fail from the shell and from the board, and `flai accept --no-release` is the only way through. The rule comes from `design/conventions/git.md`. The operator wants research findings (ADRs, design documents, new stories) to land on main and be pushed with no release. S-0052, a research story, is blocked by the operator on exactly this. Work is in `.flai-cache/worktrees/S-0053` on `story/S-0053`.

## Current state
Done on `story/S-0053` in three commits (07326b5, fb3ad4d, 3a2b0b3), ready for review.

Rule: ADR-0025, accepted format and indexed. The sentence in `design/conventions/git.md` is replaced in the template baseline and here, identical in both. `workflow.md` has a section on natures that do not release; `flai-cli.md`, `docs/users/flai.md`, `flaiover-dashboard.md`, and `docs/users/flaiover.md` agree.

flai: `release.LevelFor` returns the new level `None` for research and an error for experiment whose message names only experiment. `release.Compute` returns a skipped plan for research with the reason and `unreleased: [{component, files}]` for each component its commits touched; tags and `--deliver` change nothing. `printPlan` prints those components as landing on main without a release. Acceptance refuses an experiment in its preflight, so the preview carries a blocker and the real run refuses before the merge; before this change a refused nature was only found by the release plan, which a real acceptance computes after merging. Everything after a skipped plan (no version file, no tag, no template publish, push as usual) already existed.

Tests: `TestComputeResearch` and the `LevelFor` cases; with real git and a bare remote, `TestAcceptResearchEndToEnd` (preview, then `flai accept`: merged, archived, committed, no tag locally or on the remote, acceptance commit on the remote's main), `TestMoveResearchToDoneNamesCodeLandingUnreleased` (through `flai move done`, with a component change that lands and is named), `TestAcceptRefusesAnExperimentBeforeMerging` (blocker in the preview; the real run changes nothing locally or on the remote), and `TestAcceptPushesWhenThePlanIsSkipped`, which pins the push after a no-release acceptance and passes on the old code too, as the criterion expected. The other three fail on the old code. `make flai-test` passes, 0 lint issues.

flaiover: `UnreleasedList.svelte` under "No release" in `AcceptConfirm.svelte` and `Review.svelte`; component tests for a research preview with and without touched components, and on the review page. `make flaiover-test` (257 tests) and `make flaiover-build` pass. The preview endpoint itself is unchanged: it runs `flai accept --dry-run`, whose JSON for research is what the Go end to end test reads. Not done: a browser or container check. The change on the dashboard is one list under an existing line, the JSON it reads is pinned on the flai side, and nothing about mounts or paths changed.

S-0052 is unblocked (`flai unblock S-0052`), as this story's notes instruct. It can be accepted from the board only once this story is released and the dashboard runs the new flai; from a shell with the tree's flai it can be accepted as soon as this is on main.

## Next steps
1. The operator reviews and accepts. Then: fetch, push `main` and the tags if the board did it, wait for the release workflows, pull the image, restart the dashboard with `scripts/flai.sh`.
2. Ready behind this: S-0061, S-0059, S-0060, S-0056, in the operator's order.

## Decisions
- 2026-09-19, operator: "pull S-0053 next". It was not first in the pull order (S-0061, S-0059, S-0060, S-0056 were ahead); the operator's word overrides the order.
- 2026-09-19, operator: `experiment` is left as it is: acceptance keeps refusing it and it stays on its branch. Only `research` changes.
- 2026-09-19, agent: S-0057 was accepted with `flai accept S-0057 --by alex` on the operator's word ("I have accepted S-0057 from the board"), as the project convention allows, because nothing had been accepted: the story was still in review, main had no new commit or tag, and the dashboard's log held no request about S-0057 at all, only the operator's move of S-0053 to ready. Why the drop on done never reached the server is not known; reported to the operator.
- 2026-09-19, agent: the baseline sentence in `git.md` is edited in this story, template first, because the story's fifth criterion asks for exactly that and the operator queued and approved the story; this is not an agent changing a baseline rule on its own.

## Open questions
- A real acceptance computes its release plan after merging the story branch, so a plan error that is not about the nature (two components touched and no tag or `--deliver` to say which is delivered) still surfaces after the merge. The dry run and the dashboard's preview show it beforehand, which is why it has not bitten, but it is the same class as I-0021. Planning from the story's worktree before the merge, as the dry run does, would close it. Not in this story's scope; worth a small remediation story.
- Why the operator's acceptance of S-0057 from the board never reached the server is unknown: the 0.16.0 dashboard logged no request about S-0057, not even the acceptance preview. If it happens again on 0.17.0, the browser's console and network tab at that moment are what is needed.
- `inbox` reported this agent's own `flai unblock S-0052` back to it as a change by someone else: block and unblock intervals record no author, so the cursor cannot tell whose they are (moves can, since S-0058). Small; belongs with S-0061's work on what `inbox` reports.

## Log

### 2026-09-19T07:11:24Z
Stream opened.

### 2026-09-19T07:24:00Z
Operator decided experiment stays as it is. ADR-0025 and git.md (template baseline and here); release plans no release for research and names components landing unreleased; experiment refused in the preflight before any merge; end to end tests with real git and a bare remote; dashboard confirmation and review page list unreleased components; S-0052 unblocked. flai-test, flaiover-test (257) and the build pass. I-0010 bumped on the branch. Moving to review.
