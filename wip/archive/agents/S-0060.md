---
stream: S-0060
title: Operators create ADRs from the dashboard
updated: 2026-09-19T09:56:50Z
agent: system-flow
session: faa60a41
---

# S-0060 Operators create ADRs from the dashboard

## Context
The operator wants to record a new architecture decision from the ADRs page by writing it in markdown, with the number, file name, front matter, date, and index row supplied behind the scenes. Today ADRs are made by copying `design/adrs/0000-template.md` by hand, taking the next number by looking, and adding a row to `design/adrs/README.md` by hand; flai has no `adr` command, `flai check` does not look at the index, and the dashboard's ADRs page has no write action. This story is mostly flai: the command, the index, the check rule, and then a form that is a client of them. Work is in `.flai-cache/worktrees/S-0060` on `story/S-0060`.

## Current state
Done on `story/S-0060` in four commits (a2375e1, c34f463, 589fa37, 01f2bdb), ready for review. The branch was built on `story/S-0059` and held out of review until the operator accepted S-0059 (2026-09-19, flaiover 0.18.0, flai 1.3.1, pushed by the agent); `flai stream sync` then replayed only this story's commits onto main, with no conflicts. After the rebase `make flai-test`, `make flaiover-test` (277 tests), and the build pass, and `git merge-tree` reports a clean merge with `story/S-0063`, which is still in review, so the board can accept the two in either order.

flai: `internal/adr` and `flai adr new` / `flai adr accept`. The number is one more than the highest `NNNN-*.md` present; the file is `NNNN-slug.md` with `id`, `title` (quoted only when YAML needs it), `status`, `date`, `supersedes`, `superseded_by`, and `refines` when given; the body is the project's `0000-template.md` sections or standard input; the index row is added after the last row with the notes the index already uses; a superseded ADR gets `superseded_by` set and its row noted. `flai check` runs before and after; a finding the ADR introduces, or any failure after files were written, puts every file back, and the number is not spent. `--autocommit` makes one `docs: ADR-NNNN <title>` commit of what was written, with trailers. `--print-body` prints the template's sections. `accept` sets a proposed ADR to accepted with today's date and updates its row. `flai check` has `adr.index` (a file with no row, a row with no file, the template exempt), and this repository passes it. `flai doc` gives an accepted, superseded, or deprecated ADR the mode `none` with the reason; a proposed one and the template stay editable. Tests with real git for all of it. `make flai-test` passes, 0 lint issues.

One thing could not be tested as the fifth criterion words it: no `flai check` rule can currently be tripped by a new ADR's content (there is no link rule, the front matter is flai's, and the index row is written with the file), so the check-refusal branch of `adr new` is exercised through the same undo by a different failure (superseding an ADR that has no `superseded_by` line), which leaves a clean tree and the number unspent. The refusal by findings is the mechanism S-0059 tests for items.

flaiover: `POST /api/adrs`, `GET /api/adrs/template`, `POST /api/adrs/:id/accept`; `NewAdrForm.svelte` at `/adrs/new`; "+ new ADR" and an accept button on proposed records on `/adrs`, shown only when flai answers, with the list following the files over SSE and now showing `refines`. Component and argument tests. `make flaiover-test` (277 tests) and the build pass.

Browser check on the branch's dev server against a scratch git project with a template, one accepted ADR, and an index (removed afterwards): a proposed ADR was recorded from the form, which started from the template's four sections, and the browser landed on it in the explorer; the list showed it as proposed with an accept button; accepting it (after the confirmation) set it to accepted and removed the button; a third ADR, accepted at once, superseded 0001 and refined 0002. On disk: `0002-…` and `0003-…` with flai's front matter, the index rows "accepted, superseded by 0003" and "accepted, supersedes 0001, refines 0002", three commits by the scratch identity with the flaiover trailer (`docs: ADR-0002 …`, `docs: ADR-0002 accepted`, `docs: ADR-0003 …`), and a clean tree. With the list open, an ADR recorded through the API appeared without a reload, as proposed with its accept button. The editor endpoint answered `none` with the immutability reason for the accepted ADR and `full` for the proposed one. A status of `superseded` was refused with 400.

Documents: the decisions convention in the template baseline and here (identical), with the stale "next is 0015" project addition removed; the ADR index text here and in the template; `documentation-standard.md`; `flai-cli.md`; `flaiover-dashboard.md`; `docs/users/flai.md`; `docs/users/flaiover.md`.

## Next steps
1. The operator reviews and accepts. Then push what the board could not, wait for the release workflows, pull the image, restart the dashboard.
2. Then S-0056, the last ready story.

## Decisions
- 2026-09-19, operator: continue through the ready stories in their order; S-0060 was next.
- 2026-09-19, agent: the branch was rebased onto `story/S-0059` after `flai stream open` made it from main. When S-0059 is accepted its commits reach main, and `flai stream sync S-0060` then replays only this story's commits. If S-0059 is sent back and changes, this branch is rebased onto the new S-0059. The story is not moved to review before S-0059 is accepted, so an acceptance from the board never meets the dependency.
- 2026-09-19, agent: the immutable accepted ADR (the fourth criterion) is built here, in `flai doc`, as the story's notes allow, rather than split out: it is a few lines in the same place the form's rules live, and it answers S-0040's open question.

## Open questions
- ADR-0025 and ADR-0026 were written by hand in this session before `flai adr new` existed; from the next one on, agents use the command, as the convention now says.
- The ADR list test in flaiover asserts that every ADR in this repository is accepted. It will need a looser assertion the day a proposed ADR is committed here.
- `yamlString` quotes a title only when it has to; existing ADRs are all unquoted. If a title ever contains a double quote the JSON quoting is still valid YAML.

## Log

### 2026-09-19T09:38:15Z
Stream opened.

### 2026-09-19T09:54:44Z
T-0233 to T-0236 done and T-0237's work committed (b8e2420): flai adr new and accept, the adr.index rule, the immutable accepted ADR in flai doc, the convention in the template baseline and here, the endpoints, the form, the accept action, refines in the list, the browser check against a scratch project, documents. flai-test and flaiover-test (277) pass. Held out of review until S-0059 is accepted, because this branch is built on it.

### 2026-09-19T09:56:50Z
S-0059 accepted and pushed (flaiover 0.18.0, flai 1.3.1). Synced onto main: only this story's four commits remain, no conflicts; all suites pass; clean dry merge with story/S-0063. Criteria ticked. Moving to review.
