---
stream: S-0088
title: The Review page doesn't render markdown in the narrative pane
updated: 2026-09-22T23:51:26Z
agent: system-flow
session: cd1c877e
---

# S-0088 The Review page doesn't render markdown in the narrative pane

## Context

The narrative pane was the only place in flaiover that shows a document body with `whitespace-pre-wrap` on raw text instead of going through `$lib/markdown`'s `render`/`enhance` (the docs explorer, item page, and editors' previews all already use it).

## Current state

Done. `Review.svelte`'s `Current state` and `Next steps` sections are rendered through `render()` (docPath `wip/agents/<id>.md`) into `.prose` blocks and `enhance()`d after mount, the same shape the item page already uses. A test proves real markdown renders (a numbered list as an actual `<ol>`, bold as `<strong>`), and the pre-existing test asserting literal `1. Accept.` text was updated for the new rendering.

## Next steps
1. Move S-0088 to review; `flai check --strict` and `scripts/lint-md.sh` clean in the main checkout first.

## Decisions

## Open questions

## Log

### 2026-09-22T23:51:26Z
Stream opened.

### 2026-09-22T23:56:00Z
T-0332 done: narrative sections rendered through `$lib/markdown`'s `render`/`enhance` instead of `whitespace-pre-wrap`. `pnpm run check`/`test:unit`/`lint` clean; 370 tests pass (9 in `Review.svelte.test.ts`, including a new one that proves markdown actually renders, not just that the page shows text).

### 2026-09-23T00:00:00Z
The operator accepted S-0082 and S-0086 while this was in progress. `flai stream sync S-0088` hit a real rebase conflict in `Review.svelte.test.ts` (S-0082's own Checks section tests landed in the same file, same area, as this story's narrative test) — resolved by keeping both: the full `checks (S-0082)` describe block and this story's narrative-rendering test as siblings, `over`'s type gaining both sets of optional fields. `Review.svelte` itself merged cleanly. Rebuilt `bin/flai` after the rebase (the one built earlier was from before S-0082's merge and answered `project.info` without the new `checks` host action, briefly failing an unrelated `writes.test.ts` assertion that expects it — not a real regression, just a stale local binary). Full suite (391 tests), `svelte-check`, and lint all clean afterward. Fetched and ran `flai push --pending` for the operator's two acceptances (S-0086, S-0082; 7 commits), since nothing was still pushed automatically.
