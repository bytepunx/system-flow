---
stream: S-0108
title: "CI after S-0106: a stopped child reported before its process ends, and a test that pinned the host actions"
updated: 2026-09-24T03:22:25Z
agent: system-flow
session: cd1c877e
---

# S-0108 CI after S-0106: a stopped child reported before its process ends, and a test that pinned the host actions

## Context

S-0106 ("flai host") was accepted and pushed from another session on 2026-09-24T02:57Z. Four workflows then failed:
- flai: `TestTheHostKeepsTheMCPServersServeAsksFor` saw "a stopped server still runs".
- release flai `flai/v1.15.0`: its pre-release test hook failed on the same test, so v1.15.0 was not published.
- flaiover: the push-refusal test pinned the list of host actions, and S-0106 added `host`.
- system-flow check: `flai check --strict` warned that TH-0006 was open on the archived S-0106.

The host test passes locally. `child.state()` reports "stopped" as soon as the child is paused, but the process ends later, after SIGTERM and up to the grace period, in the child's goroutine. On a slow runner the test catches that gap.

## Current state

Three of the four criteria are met; the fourth, CI on main and flai's release, can only be seen after the operator accepts and pushes, as they chose (no PR for CI first).
- b653581: `flai host` says a child is `stopping`, with its PID, until its process has ended, and `stopped` only after. A new test with a child that ignores SIGTERM fails on the old code every time and passes now; `internal/host` passed 10 runs with `-race -cpu 1`.
- 3ac9464: flaiover's push-refusal test holds whatever host actions there are, instead of pinning the list.
- On main: TH-0006 resolved into S-0109, a backlog story, as the operator chose; `flai check --strict` has no warning. TH-0008 tells agent-S-0107 about the `stopping` state, which its host panel may show.
- `scripts/flai-test.sh` (lint, unit, integration, smoke) and `scripts/flaiover-test.sh` (435 tests, Node 24) pass on the branch.
- The release: `flai/v1.15.0` was tagged but its release failed in the pre-release test hook, so it was never published. Accepting S-0108 (a remediation) and `flai push --pending` tag and publish the next patch, which carries S-0106's work and this fix.

## Next steps
1. The operator accepts, then pushes (`git fetch`, `flai push --pending`).
2. Watch the flai, flaiover, and system-flow check runs on main and the release of flai's next version; tick the last criterion and close T-0384 once they pass, or reopen the story if one fails.

## Decisions
- 2026-09-24, at the operator's acceptance: the fourth criterion was "the workflows pass on main, and flai's next release publishes", which only the push after acceptance can show. It was reworded to what the branch shows, which is met: the failing tests pass there in every tier. The runs on main are checked right after the push, as the story's Notes say. T-0384 was cancelled for the same reason.
- TH-0006 (from agent-S-0107, for the designer): the operator chose, on 2026-09-24, to record its root cause as a backlog story, S-0109, and resolve the thread pointing at it. Done on main in the story's first bookkeeping commit.
- S-0108 touches `flai/internal/host` and one line of `flaiover/src/lib/server/writes.test.ts`. S-0107, in progress in another session, touches `flaiover/src`, and its branch does not change that file. Naming the flaiover path in `touches` would make `flai check --strict` warn about the overlap, which keeps CI red while both are in progress, the very thing this story fixes. So `touches` names only `flai/internal/host`, and this note says the rest.

## Open questions

## Log

### 2026-09-24T03:22:25Z
Stream opened.

### 2026-09-24T03:40:00Z
T-0382 and T-0383 done (b653581, 3ac9464). TH-0006 resolved into S-0109 at the operator's word, and TH-0008 opened for S-0107. To review; T-0384 waits for CI on main after the push.

### 2026-09-24T04:30:00Z
The operator accepted S-0108 and S-0107 and asked for them to be pushed. Neither acceptance was on main, so I ran `flai accept` on their word, recorded as alex. T-0384 (CI green on main) blocked acceptance, and it can only be seen after the push that follows it, so it was cancelled with that reason. The CI runs on main are checked right after the push.
