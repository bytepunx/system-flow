# Threads

Conversations between the designer and agents, one file per thread, anchored to a document, a heading, or a work item (ADR-0020). Create and answer them with `flai thread` or from the dashboard; `flai check` validates anchors and schema.
