// flaiover is a single-page app: the browser renders everything and the
// server only serves /api. See design/system/flaiover-dashboard.md, ADR-0007.
export const ssr = false;
export const prerender = false;
