# Local run of flaiover with a telemetry stack. From the repository root:
#   PROJECT=$PWD docker compose -f flaiover/compose.yaml up --build
# PROJECT is required and absolute: the repository is mounted at its host path,
# like flai dashboard does, so the links git keeps for story worktrees resolve
# in the container (ADR-0022).
# Dashboard: http://localhost:4242   Grafana (traces, metrics, logs): http://localhost:3001
services:
  flaiover:
    build:
      context: ..
      dockerfile: flaiover/Dockerfile
    image: flaiover:local
    user: '${UID:-1000}:${GID:-1000}'
    ports:
      - '4242:3000' # every interface, like flai dashboard; prefix 127.0.0.1: to restrict
    volumes:
      - '${PROJECT:?set PROJECT to the absolute path of the repository}:${PROJECT}'
      - '${PROJECT}/.flai-cache/dashboard.token:/run/secrets/flaiover_token:ro' # flai dashboard token creates it
    environment:
      PROJECT_DIR: ${PROJECT}
      FLAIOVER_TOKEN_FILE: /run/secrets/flaiover_token
      LOG_LEVEL: ${LOG_LEVEL:-info}
      OTEL_SERVICE_NAME: flaiover
      OTEL_EXPORTER_OTLP_ENDPOINT: http://lgtm:4318
      OTEL_TRACES_SAMPLER: ${OTEL_TRACES_SAMPLER:-always_on}
    depends_on:
      - lgtm
  lgtm:
    # OpenTelemetry collector, Tempo, Prometheus, Loki, and Grafana in one container for local use.
    image: grafana/otel-lgtm:latest
    ports:
      - '127.0.0.1:3001:3000'
      - '127.0.0.1:4317:4317'
      - '127.0.0.1:4318:4318'
    environment:
      ENABLE_LOGS_ALL: 'true'
